// Stub — full implementation in Task 5
export default function LocationMap(_props: { lat: number | null; lng: number | null; onPick: (lat: number, lng: number, name: string) => void }) {
  return null
}
